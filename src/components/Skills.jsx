import React from 'react';
import { SectionTitle } from './SectionTitle';

export const Skills = () => {
	return (
		<section id='skills' className='section subtitle mb-[300px]'>
			<SectionTitle section='skills' />
		</section>
	);
};
