import React from 'react';
import { SectionTitle } from './SectionTitle';

export const Skills = () => {
	return (
		<section id='skills' className='section subtitle bg-primary rounded-bl-4xl rounded-tr-4xl'>
			<SectionTitle section='skills' />
		</section>
	);
};
