import React from 'react';
import { SectionTitle } from './SectionTitle';

export const About = () => {
	return <section id="about" className="section subtitle mb-[300px]">
		<SectionTitle section="about" />
	</section>;
};
