import React from 'react';
import { SectionTitle } from './SectionTitle';

export const Projects = () => {
	return <section id="projects" className="section subtitle mb-[300px]">
		<SectionTitle section="projects" />
	</section>;
};
